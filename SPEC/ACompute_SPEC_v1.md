# ACompute Spec v1 (Foundation)

ACompute teaches computation as deterministic state transitions across abstraction levels.

Core equation:

State(t+1) = delta(State(t), Inputs(t), Control(t), Graph)
